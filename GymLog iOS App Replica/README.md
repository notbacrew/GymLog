
  # GymLog iOS App Replica

  This is a code bundle for GymLog iOS App Replica. The original project is available at https://www.figma.com/design/ug9cELezTTfsCVA94KP7fk/GymLog-iOS-App-Replica.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  